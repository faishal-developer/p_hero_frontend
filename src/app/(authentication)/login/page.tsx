import PageBody from "./PageBody";

export const metadata = {
    title: 'Quiz - login page',
    description: 'page about page description',
}

const page = () => {
    return (
        <PageBody />
    );
};

export default page;